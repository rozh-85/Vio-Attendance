# Which data backend to use: "local" (browser localStorage, default) or "supabase".
VITE_DATA_BACKEND=local

# Supabase connection (only needed when VITE_DATA_BACKEND=supabase).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
